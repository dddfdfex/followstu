<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brothers Studio — نظام المساعدين</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-database.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <!-- ZXing for barcode camera scanning -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/zxing-js/0.18.6/index.min.js"></script>
    <style>
        :root {
            --bg: #0a0f1e;
            --bg2: #0f172a;
            --surface: rgba(255,255,255,0.04);
            --surface2: rgba(255,255,255,0.07);
            --border: rgba(255,255,255,0.08);
            --border2: rgba(255,255,255,0.14);
            --blue: #3b82f6;
            --blue-dark: #2563eb;
            --green: #10b981;
            --yellow: #f59e0b;
            --red: #ef4444;
            --text: #f1f5f9;
            --muted: #64748b;
            --muted2: #94a3b8;
            --sidebar-w: 260px;
        }
        * { margin:0; padding:0; box-sizing:border-box; font-family:'Cairo',sans-serif; }
        body { background:var(--bg); color:var(--text); min-height:100vh; overflow-x:hidden; }

        /* ===== LOGIN ===== */
        #login-view {
            display:flex; align-items:center; justify-content:center;
            min-height:100vh;
            background: radial-gradient(ellipse at 30% 50%, rgba(59,130,246,0.12) 0%, transparent 60%),
                        radial-gradient(ellipse at 80% 20%, rgba(16,185,129,0.08) 0%, transparent 50%),
                        var(--bg);
        }
        .login-box {
            width:100%; max-width:420px; padding:20px;
        }
        .login-card {
            background:var(--surface2);
            border:1px solid var(--border2);
            border-radius:24px;
            padding:44px 36px;
            backdrop-filter:blur(20px);
            box-shadow: 0 32px 80px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.06);
        }
        .login-logo {
            width:72px; height:72px; border-radius:20px;
            background:linear-gradient(135deg,#3b82f6,#1d4ed8);
            display:flex; align-items:center; justify-content:center;
            font-size:30px; margin:0 auto 24px;
            box-shadow:0 8px 24px rgba(59,130,246,0.4);
        }
        .login-card h1 { text-align:center; font-size:26px; font-weight:900; margin-bottom:6px; }
        .login-card p { text-align:center; color:var(--muted2); font-size:14px; margin-bottom:32px; }
        .form-group { margin-bottom:16px; }
        .form-group label { display:block; font-size:13px; color:var(--muted2); margin-bottom:8px; font-weight:600; }
        .form-control {
            width:100%; padding:13px 16px;
            background:rgba(0,0,0,0.3); border:1px solid var(--border);
            border-radius:12px; color:var(--text); font-size:15px; font-family:'Cairo',sans-serif;
            outline:none; transition:all .25s;
        }
        .form-control:focus { border-color:var(--blue); background:rgba(0,0,0,0.5); box-shadow:0 0 0 3px rgba(59,130,246,0.15); }
        .btn {
            display:inline-flex; align-items:center; justify-content:center; gap:8px;
            padding:13px 20px; border:none; border-radius:12px;
            font-family:'Cairo',sans-serif; font-size:15px; font-weight:800;
            cursor:pointer; transition:all .25s; width:100%;
        }
        .btn-primary { background:var(--blue); color:#fff; }
        .btn-primary:hover { background:var(--blue-dark); transform:translateY(-1px); box-shadow:0 8px 20px rgba(59,130,246,0.35); }
        .btn-success { background:var(--green); color:#fff; }
        .btn-success:hover { background:#059669; }
        .btn-danger { background:var(--red); color:#fff; }
        .btn-warning { background:var(--yellow); color:#000; }
        .btn-sm { padding:7px 14px; font-size:12px; width:auto; border-radius:8px; }
        .btn-ghost {
            background:var(--surface2); border:1px solid var(--border); color:var(--muted2);
        }
        .btn-ghost:hover { border-color:var(--border2); color:var(--text); }
        .error-box {
            background:rgba(239,68,68,0.12); border:1px solid rgba(239,68,68,0.3);
            color:#fca5a5; border-radius:10px; padding:12px 16px;
            font-size:13px; margin-bottom:16px; display:none;
        }

        /* ===== APP SHELL ===== */
        #app-shell { display:none; min-height:100vh; }
        .layout { display:flex; min-height:100vh; }

        /* ===== SIDEBAR ===== */
        .sidebar {
            width:var(--sidebar-w); flex-shrink:0;
            background:rgba(15,23,42,0.95);
            border-left:1px solid var(--border);
            display:flex; flex-direction:column;
            padding:0; position:fixed; top:0; right:0;
            height:100vh; z-index:50; overflow:hidden;
        }
        .sidebar-header {
            padding:24px 20px 20px;
            border-bottom:1px solid var(--border);
        }
        .sidebar-brand {
            display:flex; align-items:center; gap:12px;
        }
        .brand-icon {
            width:40px; height:40px; border-radius:11px;
            background:linear-gradient(135deg,#3b82f6,#1d4ed8);
            display:flex; align-items:center; justify-content:center; font-size:18px;
            flex-shrink:0;
        }
        .brand-text { font-size:17px; font-weight:900; line-height:1.2; }
        .brand-sub { font-size:10px; color:var(--muted2); font-weight:600; letter-spacing:.5px; }
        .sidebar-user {
            padding:16px 20px; border-bottom:1px solid var(--border);
            display:flex; align-items:center; gap:10px;
        }
        .user-avatar {
            width:36px; height:36px; border-radius:50%;
            background:linear-gradient(135deg,#10b981,#059669);
            display:flex; align-items:center; justify-content:center;
            font-size:14px; flex-shrink:0;
        }
        .user-name { font-size:13px; font-weight:700; }
        .user-role { font-size:11px; color:var(--muted2); }
        .sidebar-nav { flex:1; padding:16px 12px; overflow-y:auto; display:flex; flex-direction:column; gap:4px; }
        .nav-item {
            display:flex; align-items:center; gap:12px;
            padding:12px 14px; border-radius:11px; cursor:pointer;
            color:var(--muted2); font-size:14px; font-weight:600;
            transition:all .2s; border:1px solid transparent;
        }
        .nav-item:hover { color:var(--text); background:var(--surface2); }
        .nav-item.active { color:#fff; background:var(--blue); border-color:rgba(255,255,255,0.1); }
        .nav-item i { width:18px; text-align:center; font-size:15px; }
        .nav-divider { height:1px; background:var(--border); margin:8px 0; }
        .nav-logout { color:var(--red) !important; }
        .nav-logout:hover { background:rgba(239,68,68,0.1) !important; border-color:rgba(239,68,68,0.2) !important; }
        .sidebar-footer { padding:16px 20px; border-top:1px solid var(--border); }
        .timer-badge {
            display:flex; align-items:center; gap:8px;
            font-size:12px; color:var(--muted2);
        }
        .timer-dot { width:6px; height:6px; border-radius:50%; background:var(--green); animation:pulse 2s infinite; }
        @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.4} }

        /* ===== MAIN CONTENT ===== */
        .main {
            flex:1; margin-right:var(--sidebar-w);
            display:flex; flex-direction:column; min-height:100vh;
        }
        .topbar {
            position:sticky; top:0; z-index:40;
            background:rgba(10,15,30,0.9); backdrop-filter:blur(12px);
            border-bottom:1px solid var(--border);
            padding:16px 28px;
            display:flex; align-items:center; justify-content:space-between; gap:16px;
        }
        .topbar-title { font-size:18px; font-weight:800; }
        .search-bar {
            display:flex; align-items:center; gap:10px;
            background:rgba(0,0,0,0.3); border:1px solid var(--border);
            border-radius:30px; padding:9px 16px; flex:1; max-width:380px;
        }
        .search-bar input {
            border:none; background:transparent; color:var(--text);
            font-family:'Cairo',sans-serif; font-size:13px; outline:none; flex:1;
        }
        .search-bar i { color:var(--muted); font-size:13px; }
        .content { flex:1; padding:28px; display:flex; flex-direction:column; gap:24px; }

        /* ===== VIEWS ===== */
        .view { display:none; flex-direction:column; gap:24px; animation:fadeIn .3s ease; }
        .view.active { display:flex; }
        @keyframes fadeIn { from{opacity:0;transform:translateY(8px)} to{opacity:1;transform:translateY(0)} }

        /* ===== CARDS ===== */
        .card {
            background:var(--surface); border:1px solid var(--border);
            border-radius:18px; padding:24px;
            backdrop-filter:blur(10px);
        }
        .card-header { display:flex; align-items:center; justify-content:space-between; margin-bottom:20px; }
        .card-title { font-size:15px; font-weight:800; display:flex; align-items:center; gap:10px; }
        .card-title i { color:var(--blue); }

        /* ===== STATS GRID ===== */
        .stats-grid { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; }
        .stat-card {
            background:var(--surface); border:1px solid var(--border);
            border-radius:16px; padding:20px; text-align:center; transition:all .3s;
        }
        .stat-card:hover { transform:translateY(-2px); border-color:var(--border2); }
        .stat-card.green { border-color:rgba(16,185,129,0.3); }
        .stat-card.red { border-color:rgba(239,68,68,0.3); }
        .stat-card.yellow { border-color:rgba(245,158,11,0.3); }
        .stat-icon { font-size:24px; margin-bottom:10px; }
        .stat-val { font-size:38px; font-weight:900; line-height:1; }
        .stat-label { font-size:12px; color:var(--muted2); margin-top:6px; font-weight:600; }
        .green .stat-val { color:var(--green); }
        .red .stat-val { color:var(--red); }
        .yellow .stat-val { color:var(--yellow); }

        /* ===== TABLES ===== */
        .table-wrap { overflow-x:auto; }
        table { width:100%; border-collapse:collapse; }
        th, td { padding:13px 16px; text-align:right; font-size:13px; }
        th { color:var(--muted2); font-weight:600; border-bottom:1px solid var(--border); }
        td { border-bottom:1px solid rgba(255,255,255,0.03); }
        tr:last-child td { border-bottom:none; }
        tr:hover td { background:rgba(255,255,255,0.02); }

        /* ===== GRID ===== */
        .grid-2 { display:grid; grid-template-columns:1fr 1fr; gap:20px; }
        .grid-3 { display:grid; grid-template-columns:1fr 1fr 1fr; gap:20px; }

        /* ===== FORM ===== */
        .form-row { display:grid; grid-template-columns:repeat(auto-fit,minmax(180px,1fr)); gap:16px; margin-bottom:16px; }
        .form-group-in { display:flex; flex-direction:column; gap:6px; }
        .form-label { font-size:12px; color:var(--muted2); font-weight:600; }
        .form-control-sm { padding:10px 14px; border-radius:10px; font-size:13px; }
        select.form-control { appearance:none; cursor:pointer; }

        /* ===== ATTENDANCE SCAN ZONE ===== */
        .scan-zone {
            border:2px dashed var(--green);
            border-radius:16px; padding:24px;
            text-align:center; position:relative;
            background:rgba(16,185,129,0.04);
            transition:all .3s;
        }
        .scan-zone.scanning { border-color:var(--blue); background:rgba(59,130,246,0.04); }
        .scan-big-input {
            width:100%; padding:18px 20px;
            background:rgba(0,0,0,0.4); border:2px solid var(--green);
            border-radius:14px; color:var(--text);
            font-family:'Cairo',sans-serif; font-size:20px; font-weight:800;
            outline:none; text-align:center; letter-spacing:4px;
            transition:all .25s;
        }
        .scan-big-input:focus { border-color:var(--blue); box-shadow:0 0 0 4px rgba(59,130,246,0.2); }
        .scan-btns { display:flex; gap:10px; margin-top:14px; justify-content:center; }

        /* Camera modal */
        .modal-overlay {
            position:fixed; inset:0; background:rgba(0,0,0,0.85);
            z-index:200; display:none; align-items:center; justify-content:center;
            backdrop-filter:blur(8px);
        }
        .modal-overlay.open { display:flex; }
        .modal-box {
            background:#0f172a; border:1px solid var(--border2);
            border-radius:24px; padding:28px; max-width:500px; width:95%;
        }
        .modal-title { font-size:17px; font-weight:800; margin-bottom:20px; display:flex; align-items:center; gap:10px; }
        #camera-video {
            width:100%; border-radius:14px; background:#000;
            aspect-ratio:4/3; object-fit:cover;
            border:2px solid var(--border);
        }
        .camera-overlay-line {
            position:absolute; top:50%; left:10%; right:10%;
            height:2px; background:var(--green);
            box-shadow:0 0 10px var(--green);
            animation:scanLine 2s ease-in-out infinite;
        }
        @keyframes scanLine { 0%{top:20%} 50%{top:80%} 100%{top:20%} }
        .camera-wrapper { position:relative; }
        .camera-status {
            margin-top:14px; padding:12px; border-radius:10px;
            text-align:center; font-weight:700; font-size:14px;
            background:rgba(59,130,246,0.1); border:1px solid rgba(59,130,246,0.2);
            color:#93c5fd;
        }

        /* ===== FEEDBACK ALERT ===== */
        .feedback-alert {
            padding:18px 20px; border-radius:14px;
            font-weight:800; font-size:16px; text-align:center;
            display:none; animation:alertIn .3s ease;
        }
        @keyframes alertIn { from{transform:scale(.95);opacity:0} to{transform:scale(1);opacity:1} }
        .feedback-alert.success { background:rgba(16,185,129,0.15); border:1px solid rgba(16,185,129,0.3); color:#a7f3d0; }
        .feedback-alert.error { background:rgba(239,68,68,0.15); border:1px solid rgba(239,68,68,0.3); color:#fca5a5; }
        .feedback-alert.warning { background:rgba(245,158,11,0.15); border:1px solid rgba(245,158,11,0.3); color:#fde68a; }

        /* ===== ID CARD ===== */
        .id-card {
            background:linear-gradient(135deg,#1e3a8a 0%,#0f172a 100%);
            border:2px solid rgba(255,255,255,0.1);
            border-radius:20px; padding:28px;
            max-width:320px; margin:0 auto;
            text-align:center; display:flex; flex-direction:column;
            align-items:center; gap:12px;
            box-shadow:0 20px 60px rgba(0,0,0,0.5);
        }
        .id-avatar { width:64px; height:64px; border-radius:50%; background:rgba(255,255,255,0.1); display:flex; align-items:center; justify-content:center; font-size:26px; }
        .id-prof { font-weight:900; font-size:17px; }
        .id-name { font-size:14px; color:#93c5fd; font-weight:700; }
        .id-code { font-size:12px; color:rgba(255,255,255,0.6); }
        .id-qr { background:#fff; padding:10px; border-radius:10px; }
        .id-badge { background:rgba(59,130,246,0.2); border:1px solid rgba(59,130,246,0.3); color:#93c5fd; padding:4px 12px; border-radius:20px; font-size:11px; font-weight:700; }

        /* ===== TIMELINE ===== */
        .timeline { display:flex; flex-direction:column; gap:8px; max-height:320px; overflow-y:auto; }
        .tl-item {
            padding:12px 14px; border-radius:10px;
            background:rgba(0,0,0,0.2); border-right:3px solid var(--blue);
            font-size:13px;
        }
        .tl-meta { display:flex; justify-content:space-between; color:var(--muted); font-size:11px; margin-top:4px; }

        /* ===== SEARCH RESULTS ===== */
        #search-results-panel {
            position:absolute; top:100%; right:0; left:0; z-index:100;
            background:#0f172a; border:1px solid var(--border2);
            border-radius:16px; margin-top:8px; overflow:hidden;
            box-shadow:0 20px 60px rgba(0,0,0,0.6); display:none;
        }
        .search-results-header { padding:14px 16px; border-bottom:1px solid var(--border); display:flex; justify-content:space-between; align-items:center; }
        .search-pos { position:relative; }

        /* ===== BADGES ===== */
        .badge { padding:3px 10px; border-radius:20px; font-size:11px; font-weight:700; }
        .badge-green { background:rgba(16,185,129,0.15); color:#6ee7b7; border:1px solid rgba(16,185,129,0.2); }
        .badge-red { background:rgba(239,68,68,0.15); color:#fca5a5; border:1px solid rgba(239,68,68,0.2); }
        .badge-yellow { background:rgba(245,158,11,0.15); color:#fde68a; border:1px solid rgba(245,158,11,0.2); }
        .badge-blue { background:rgba(59,130,246,0.15); color:#93c5fd; border:1px solid rgba(59,130,246,0.2); }

        /* ===== WALLET ===== */
        .wallet-big { font-size:48px; font-weight:900; color:var(--green); text-align:center; }

        /* ===== LOW BALANCE WARNING ===== */
        .low-balance-row td:last-child { color:var(--red); font-weight:800; }

        /* ===== SCROLLBAR ===== */
        ::-webkit-scrollbar { width:5px; }
        ::-webkit-scrollbar-track { background:transparent; }
        ::-webkit-scrollbar-thumb { background:var(--border2); border-radius:999px; }

        /* ===== FOOTER ===== */
        .footer {
            text-align:center; padding:14px; font-size:12px; color:var(--muted);
            border-top:1px solid var(--border); font-weight:600;
        }

        /* ===== RESPONSIVE ===== */
        @media(max-width:900px) {
            .sidebar { width:100%; height:auto; position:relative; }
            .main { margin-right:0; }
            .layout { flex-direction:column; }
            .stats-grid { grid-template-columns:repeat(2,1fr); }
            .grid-2 { grid-template-columns:1fr; }
        }

        /* ===== WHATSAPP BTN ===== */
        .wa-btn {
            display:inline-flex; align-items:center; gap:5px;
            background:#25d366; color:#fff; padding:5px 10px;
            border-radius:8px; font-size:11px; font-weight:700;
            text-decoration:none; white-space:nowrap;
        }
        .wa-btn:hover { background:#128c7e; }

        /* assistant tracking */
        .track-btn {
            display:inline-flex; align-items:center; gap:5px;
            background:var(--blue); color:#fff; padding:5px 10px;
            border-radius:8px; font-size:11px; font-weight:700; cursor:pointer; border:none;
        }
    </style>
</head>
<body>

<!-- ===================== LOGIN ===================== -->
<div id="login-view">
    <div class="login-box">
        <div class="login-card">
            <div class="login-logo">🎓</div>
            <h1>Brothers Studio</h1>
            <p>نظام إدارة الحضور والاشتراكات</p>
            <div id="login-error" class="error-box"></div>
            <form onsubmit="doLogin(event)">
                <div class="form-group">
                    <label>كود الدخول</label>
                    <input type="password" id="login-code" class="form-control" placeholder="أدخل كود المساعد" required autocomplete="off">
                </div>
                <button type="submit" class="btn btn-primary">
                    <i class="fa-solid fa-right-to-bracket"></i> دخول النظام
                </button>
            </form>
        </div>
    </div>
</div>

<!-- ===================== APP ===================== -->
<div id="app-shell">
    <div class="layout">

        <!-- SIDEBAR -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-brand">
                    <div class="brand-icon">🎓</div>
                    <div>
                        <div class="brand-text">Brothers Studio</div>
                        <div class="brand-sub">ATTENDANCE SYSTEM</div>
                    </div>
                </div>
            </div>
            <div class="sidebar-user">
                <div class="user-avatar">👤</div>
                <div>
                    <div class="user-name" id="sb-user-name">مساعد</div>
                    <div class="user-role" id="sb-user-role">مساعد معتمد</div>
                </div>
            </div>
            <nav class="sidebar-nav">
                <div class="nav-item active" id="nav-summary" onclick="showView('summary')">
                    <i class="fa-solid fa-chart-pie"></i> لوحة الإحصائيات
                </div>
                <div class="nav-item" id="nav-sessions" onclick="showView('sessions')">
                    <i class="fa-solid fa-id-badge"></i> تسجيل الحضور
                </div>
                <div class="nav-item" id="nav-students" onclick="showView('students')">
                    <i class="fa-solid fa-graduation-cap"></i> الطلاب
                </div>
                <div class="nav-item" id="nav-wallet" onclick="showView('wallet')">
                    <i class="fa-solid fa-wallet"></i> المحفظة
                </div>
                <div class="nav-divider"></div>
                <div class="nav-item nav-logout" onclick="doLogout()">
                    <i class="fa-solid fa-power-off"></i> خروج
                </div>
            </nav>
            <div class="sidebar-footer">
                <div class="timer-badge">
                    <div class="timer-dot"></div>
                    <span id="session-timer">الجلسة: 00:00</span>
                </div>
            </div>
        </aside>

        <!-- MAIN -->
        <div class="main">
            <header class="topbar">
                <div class="topbar-title" id="page-title">لوحة الإحصائيات</div>
                <div class="search-pos">
                    <div class="search-bar">
                        <i class="fa-solid fa-magnifying-glass"></i>
                        <input type="text" id="search-inp" placeholder="بحث بالاسم، الكود، الهاتف..." oninput="doSearch(this.value)">
                    </div>
                    <div id="search-results-panel">
                        <div class="search-results-header">
                            <span style="font-size:13px;font-weight:700;color:var(--blue)"><i class="fa-solid fa-bolt"></i> نتائج البحث</span>
                            <button class="btn btn-sm btn-ghost" onclick="closeSearch()">إغلاق</button>
                        </div>
                        <div class="table-wrap" style="max-height:300px;overflow-y:auto">
                            <table id="search-table">
                                <thead><tr><th>الكود</th><th>الاسم</th><th>الهاتف</th><th>المجموعة</th><th>الرصيد</th></tr></thead>
                                <tbody id="search-tbody"></tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <div style="font-size:13px;color:var(--muted2)" id="prof-display">الأستاذ: —</div>
            </header>

            <div class="content">

                <!-- ===== SUMMARY VIEW ===== -->
                <div class="view active" id="view-summary">
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-icon" style="color:var(--blue)">👥</div>
                            <div class="stat-val" id="s-total">0</div>
                            <div class="stat-label">إجمالي الطلاب</div>
                        </div>
                        <div class="stat-card green">
                            <div class="stat-icon">✅</div>
                            <div class="stat-val" id="s-present">0</div>
                            <div class="stat-label">حضور اليوم</div>
                        </div>
                        <div class="stat-card red">
                            <div class="stat-icon">❌</div>
                            <div class="stat-val" id="s-absent">0</div>
                            <div class="stat-label">غياب اليوم</div>
                        </div>
                        <div class="stat-card yellow">
                            <div class="stat-icon">📚</div>
                            <div class="stat-val" id="s-groups">0</div>
                            <div class="stat-label">المجموعات</div>
                        </div>
                    </div>

                    <div class="grid-2">
                        <div class="card">
                            <div class="card-header">
                                <div class="card-title"><i class="fa-solid fa-battery-quarter" style="color:var(--red)"></i> رصيد شارف على الانتهاء</div>
                            </div>
                            <div class="table-wrap">
                                <table>
                                    <thead><tr><th>الكود</th><th>الاسم</th><th>الرصيد</th><th>واتساب</th></tr></thead>
                                    <tbody id="tbl-low-balance"></tbody>
                                </table>
                            </div>
                        </div>
                        <div class="card">
                            <div class="card-header">
                                <div class="card-title"><i class="fa-solid fa-clock-rotate-left"></i> سجل الأنشطة</div>
                            </div>
                            <div class="timeline" id="timeline"></div>
                        </div>
                    </div>
                </div>

                <!-- ===== SESSIONS VIEW ===== -->
                <div class="view" id="view-sessions">
                    <div class="card">
                        <div class="card-header">
                            <div class="card-title"><i class="fa-solid fa-barcode"></i> تسجيل الحضور الفوري</div>
                        </div>
                        <div class="form-row" style="margin-bottom:20px">
                            <div class="form-group-in">
                                <label class="form-label">المجموعة المستهدفة</label>
                                <select id="att-group" class="form-control" onchange="syncAttendance()">
                                    <option value="">— اختر المجموعة —</option>
                                </select>
                            </div>
                        </div>

                        <div class="scan-zone" id="scan-zone">
                            <p style="margin-bottom:14px;color:var(--muted2);font-size:13px">امسح الباركود بجهاز قارئ خارجي أو افتح الكاميرا</p>
                            <input type="text" id="scan-input" class="scan-big-input" placeholder="• • • • • • •" onkeypress="onScanKey(event)" autofocus>
                            <div class="scan-btns">
                                <button class="btn btn-success btn-sm" onclick="openCamera()">
                                    <i class="fa-solid fa-camera"></i> مسح بالكاميرا
                                </button>
                                <button class="btn btn-primary btn-sm" onclick="manualScan()">
                                    <i class="fa-solid fa-check"></i> تأكيد يدوي
                                </button>
                            </div>
                        </div>

                        <div id="att-feedback" class="feedback-alert" style="margin-top:16px"></div>
                    </div>

                    <div class="card">
                        <div class="card-header">
                            <div class="card-title"><i class="fa-solid fa-list-check"></i> تقرير الحضور والغياب</div>
                            <div style="display:flex;gap:8px">
                                <button class="btn btn-sm btn-danger" onclick="exportReport('pdf')"><i class="fa-solid fa-file-pdf"></i> PDF</button>
                                <button class="btn btn-sm btn-success" onclick="exportReport('excel')"><i class="fa-solid fa-file-excel"></i> Excel</button>
                            </div>
                        </div>
                        <div class="table-wrap">
                            <table id="tbl-report">
                                <thead>
                                    <tr>
                                        <th>الكود</th><th>الاسم</th><th>الحالة</th><th>الرصيد</th><th>ولي الأمر</th>
                                    </tr>
                                </thead>
                                <tbody id="report-tbody"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- ===== STUDENTS VIEW ===== -->
                <div class="view" id="view-students">
                    <div class="grid-2">
                        <div class="card">
                            <div class="card-title" style="margin-bottom:20px"><i class="fa-solid fa-user-plus"></i> تسجيل طالب جديد</div>
                            <form onsubmit="addStudent(event)">
                                <div class="form-row">
                                    <div class="form-group-in">
                                        <label class="form-label">الاسم الكامل</label>
                                        <input type="text" id="st-name" class="form-control" required>
                                    </div>
                                    <div class="form-group-in">
                                        <label class="form-label">هاتف الطالب</label>
                                        <input type="tel" id="st-phone" class="form-control" required>
                                    </div>
                                </div>
                                <div class="form-row">
                                    <div class="form-group-in">
                                        <label class="form-label">هاتف ولي الأمر</label>
                                        <input type="tel" id="st-parent" class="form-control" required>
                                    </div>
                                    <div class="form-group-in">
                                        <label class="form-label">الرصيد المبدئي (حصص)</label>
                                        <input type="number" id="st-wallet" class="form-control" value="0" min="0" required>
                                    </div>
                                </div>
                                <div class="form-row">
                                    <div class="form-group-in">
                                        <label class="form-label">السنتر</label>
                                        <select id="st-center" class="form-control" required></select>
                                    </div>
                                    <div class="form-group-in">
                                        <label class="form-label">المجموعة</label>
                                        <select id="st-group" class="form-control" required></select>
                                    </div>
                                </div>
                                <button type="submit" class="btn btn-primary"><i class="fa-solid fa-save"></i> حفظ وإصدار البطاقة</button>
                            </form>
                        </div>

                        <div class="card" style="align-items:center;justify-content:center;display:flex;flex-direction:column;gap:16px">
                            <div class="card-title" style="margin-bottom:0"><i class="fa-solid fa-id-card"></i> البطاقة الذكية</div>
                            <div id="id-card-zone" style="display:none;width:100%">
                                <div class="id-card">
                                    <div class="id-avatar">🎓</div>
                                    <div class="id-badge">Brothers Studio</div>
                                    <div class="id-prof" id="id-prof-name">—</div>
                                    <div class="id-name" id="id-st-name">—</div>
                                    <div class="id-code" id="id-st-code">كود: —</div>
                                    <div class="id-qr" id="id-qr-zone"></div>
                                </div>
                            </div>
                            <div id="id-empty" style="text-align:center;color:var(--muted);font-size:13px">
                                <i class="fa-solid fa-id-card" style="font-size:40px;margin-bottom:10px;display:block"></i>
                                ستظهر البطاقة هنا بعد التسجيل
                            </div>
                            <button class="btn btn-primary btn-sm" id="print-btn" style="display:none" onclick="window.print()">
                                <i class="fa-solid fa-print"></i> طباعة البطاقة
                            </button>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-title" style="margin-bottom:16px"><i class="fa-solid fa-users"></i> قائمة الطلاب</div>
                        <div class="table-wrap">
                            <table id="tbl-students">
                                <thead>
                                    <tr><th>الكود</th><th>الاسم</th><th>السنتر</th><th>المجموعة</th><th>الرصيد</th><th>البطاقة</th></tr>
                                </thead>
                                <tbody id="students-tbody"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- ===== WALLET VIEW ===== -->
                <div class="view" id="view-wallet">
                    <div class="card" style="max-width:560px;margin:0 auto;width:100%">
                        <div class="card-title" style="margin-bottom:20px;justify-content:center"><i class="fa-solid fa-money-bill-transfer"></i> شحن وخصم المحفظة</div>
                        <div class="form-group-in" style="margin-bottom:16px">
                            <label class="form-label">كود الطالب</label>
                            <input type="text" id="w-code" class="form-control" placeholder="أدخل الكود" oninput="previewWallet()">
                        </div>
                        <div id="wallet-preview" style="display:none;text-align:center;padding:20px;margin-bottom:16px">
                            <div style="font-size:13px;color:var(--muted2);margin-bottom:6px" id="w-st-name">—</div>
                            <div class="wallet-big" id="w-balance">0</div>
                            <div style="font-size:12px;color:var(--muted2);margin-top:4px">الرصيد الحالي (حصص)</div>
                        </div>
                        <div class="form-row">
                            <div class="form-group-in">
                                <label class="form-label">نوع العملية</label>
                                <select id="w-action" class="form-control">
                                    <option value="add">شحن (إضافة حصص)</option>
                                    <option value="deduct">خصم (سحب حصص)</option>
                                    <option value="set">تعديل مباشر</option>
                                </select>
                            </div>
                            <div class="form-group-in">
                                <label class="form-label">عدد الحصص</label>
                                <input type="number" id="w-amount" class="form-control" min="0" value="1">
                            </div>
                        </div>
                        <button class="btn btn-primary" onclick="doWallet()"><i class="fa-solid fa-rotate"></i> تنفيذ العملية</button>
                        <div id="wallet-feedback" style="margin-top:14px;text-align:center;font-weight:700;font-size:14px"></div>
                    </div>
                </div>

            </div>
            <div class="footer">Developed by Brothers Studio OS &nbsp;|&nbsp; نظام المساعدين v2.0</div>
        </div>
    </div>
</div>

<!-- ===== CAMERA MODAL ===== -->
<div class="modal-overlay" id="camera-modal">
    <div class="modal-box">
        <div class="modal-title"><i class="fa-solid fa-camera" style="color:var(--blue)"></i> مسح الباركود بالكاميرا</div>
        <div class="camera-wrapper">
            <video id="camera-video" playsinline autoplay muted></video>
            <div class="camera-overlay-line"></div>
        </div>
        <div class="camera-status" id="cam-status">جاري البحث عن باركود...</div>
        <div style="display:flex;gap:10px;margin-top:16px">
            <button class="btn btn-danger" onclick="closeCamera()"><i class="fa-solid fa-times"></i> إغلاق الكاميرا</button>
        </div>
    </div>
</div>

<script>
// ===== FIREBASE =====
const firebaseConfig = {
    apiKey: "AIzaSyAMBxp98hk1C9K7sEF8et0JpwzLWjTPZ_4",
    authDomain: "omnia-store-7c8e4.firebaseapp.com",
    databaseURL: "https://omnia-store-7c8e4-default-rtdb.europe-west1.firebasedatabase.app",
    projectId: "omnia-store-7c8e4",
    storageBucket: "omnia-store-7c8e4.firebasestorage.app",
    messagingSenderId: "840762940236",
    appId: "1:840762940236:web:b5ea870c11e475e9ab8de9"
};
firebase.initializeApp(firebaseConfig);
const db = firebase.database();

// ===== STATE =====
let session = null;
let students = {};
let groups = {};
let centers = {};
let sessionStart = null;
let timerInterval = null;
let codeReader = null;
let cameraStream = null;
let failedAttempts = {};

// ===== AUTH =====
window.onload = () => {
    const saved = localStorage.getItem('bs_session');
    if (saved) {
        session = JSON.parse(saved);
        startApp();
    }
};

function doLogin(e) {
    e.preventDefault();
    const code = document.getElementById('login-code').value.trim();
    const errEl = document.getElementById('login-error');

    if (failedAttempts[code] >= 5) {
        errEl.style.display='block';
        errEl.innerText='تم حظر هذا الكود مؤقتاً لكثرة المحاولات الخاطئة';
        return;
    }

    db.ref('app_configurations/assistants').once('value', snap => {
        let found = false;
        if (snap.exists()) {
            snap.forEach(child => {
                const a = child.val();
                if (String(a.code) === code) {
                    session = { name: a.name, role: 'مساعد معتمد', id: child.key };
                    found = true;
                }
            });
        }
        if (found) {
            localStorage.setItem('bs_session', JSON.stringify(session));
            // Log assistant login with timestamp
            db.ref(`assistant_logs/${session.id}`).push({
                action: 'login', time: new Date().toISOString(),
                date: new Date().toLocaleDateString('ar-EG')
            });
            startApp();
        } else {
            failedAttempts[code] = (failedAttempts[code] || 0) + 1;
            errEl.style.display='block';
            errEl.innerText='كود غير صحيح. يرجى المحاولة مرة أخرى.';
        }
    });
}

function doLogout() {
    if (session?.id) {
        db.ref(`assistant_logs/${session.id}`).push({
            action: 'logout', time: new Date().toISOString(),
            date: new Date().toLocaleDateString('ar-EG')
        });
    }
    log('تسجيل خروج من النظام');
    localStorage.removeItem('bs_session');
    session = null;
    if (timerInterval) clearInterval(timerInterval);
    document.getElementById('app-shell').style.display='none';
    document.getElementById('login-view').style.display='flex';
    document.getElementById('login-code').value='';
}

function startApp() {
    document.getElementById('login-view').style.display='none';
    document.getElementById('app-shell').style.display='block';
    document.getElementById('sb-user-name').innerText = session.name;
    document.getElementById('sb-user-role').innerText = session.role;
    sessionStart = Date.now();
    timerInterval = setInterval(updateTimer, 1000);
    log('دخول النظام', session.name);
    syncAll();
}

function updateTimer() {
    const elapsed = Math.floor((Date.now()-sessionStart)/1000);
    const m = String(Math.floor(elapsed/60)).padStart(2,'0');
    const s = String(elapsed%60).padStart(2,'0');
    document.getElementById('session-timer').innerText = `الجلسة: ${m}:${s}`;
}

// ===== SYNC =====
function syncAll() {
    db.ref('app_configurations/professor_identity').on('value', snap => {
        const n = snap.val() || '—';
        document.getElementById('prof-display').innerText = `الأستاذ: ${n}`;
    });
    db.ref('app_configurations/centers').on('value', snap => {
        centers = snap.val() || {};
        rebuildSelects();
    });
    db.ref('app_configurations/groups').on('value', snap => {
        groups = snap.val() || {};
        rebuildSelects();
        syncAttendance();
    });
    db.ref('students_directory').on('value', snap => {
        students = snap.val() || {};
        document.getElementById('s-total').innerText = Object.keys(students).length;
        document.getElementById('s-groups').innerText = Object.keys(groups).length;
        renderStudentsTable();
        renderLowBalance();
        syncAttendance();
    });
    db.ref('system_operations_logs').limitToLast(20).on('value', snap => {
        const el = document.getElementById('timeline');
        el.innerHTML = '';
        if (!snap.exists()) return;
        const items = [];
        snap.forEach(c => items.unshift(c.val()));
        items.forEach(l => {
            el.innerHTML += `<div class="tl-item"><strong>${l.action}</strong><div class="tl-meta"><span>${l.user||'نظام'}</span><span>${l.timestamp}</span></div></div>`;
        });
    });
}

function rebuildSelects() {
    ['st-center','att-group'].forEach(id => {
        const el = document.getElementById(id);
        if (!el) return;
        const prev = el.value;
        if (id === 'att-group') {
            el.innerHTML = '<option value="">— اختر المجموعة —</option>';
            Object.entries(groups).forEach(([k,v]) => {
                el.innerHTML += `<option value="${k}">${v.title}</option>`;
            });
        } else {
            el.innerHTML = '';
            Object.entries(centers).forEach(([k,v]) => {
                el.innerHTML += `<option value="${k}">${v.title}</option>`;
            });
        }
        if (prev) el.value = prev;
    });
    const sg = document.getElementById('st-group');
    if (sg) {
        sg.innerHTML = '';
        Object.entries(groups).forEach(([k,v]) => {
            sg.innerHTML += `<option value="${k}">${v.title}</option>`;
        });
    }
}

function log(action, user) {
    const now = new Date();
    db.ref('system_operations_logs').push({
        action, user: user || (session?.name || 'نظام'),
        timestamp: now.toLocaleDateString('ar-EG') + ' ' + now.toLocaleTimeString('ar-EG')
    });
}

// ===== VIEWS =====
const viewTitles = { summary:'لوحة الإحصائيات', sessions:'تسجيل الحضور', students:'إدارة الطلاب', wallet:'المحفظة' };

function showView(v) {
    document.querySelectorAll('.view').forEach(el => el.classList.remove('active'));
    document.querySelectorAll('.nav-item').forEach(el => el.classList.remove('active'));
    document.getElementById('view-'+v).classList.add('active');
    document.getElementById('nav-'+v).classList.add('active');
    document.getElementById('page-title').innerText = viewTitles[v] || v;
}

// ===== SEARCH =====
function doSearch(q) {
    const panel = document.getElementById('search-results-panel');
    const tbody = document.getElementById('search-tbody');
    if (!q.trim()) { panel.style.display='none'; return; }
    q = q.toLowerCase();
    tbody.innerHTML = '';
    let found = false;
    Object.values(students).forEach(s => {
        const g = groups[s.group_reference]?.title || '';
        if ([s.name,s.uid,s.student_phone,s.parent_phone,g].some(x=>x.toLowerCase().includes(q))) {
            found = true;
            tbody.innerHTML += `<tr><td><b>${s.uid}</b></td><td>${s.name}</td><td>${s.student_phone}</td><td>${g}</td><td><span class="badge badge-green">${s.wallet_balance} حصة</span></td></tr>`;
        }
    });
    if (!found) tbody.innerHTML = '<tr><td colspan="5" style="text-align:center;color:var(--muted)">لا توجد نتائج</td></tr>';
    panel.style.display='block';
}

function closeSearch() {
    document.getElementById('search-inp').value='';
    document.getElementById('search-results-panel').style.display='none';
}

// ===== TABLES =====
function renderStudentsTable() {
    const tbody = document.getElementById('students-tbody');
    tbody.innerHTML = '';
    Object.values(students).forEach(s => {
        const c = centers[s.center_reference]?.title || '—';
        const g = groups[s.group_reference]?.title || '—';
        tbody.innerHTML += `<tr>
            <td><b>${s.uid}</b></td><td>${s.name}</td><td>${c}</td><td>${g}</td>
            <td><span class="badge badge-green">${s.wallet_balance} حصة</span></td>
            <td><button class="btn btn-sm btn-ghost" onclick='showIDCard(${JSON.stringify(s)})'><i class="fa-solid fa-id-card"></i></button></td>
        </tr>`;
    });
}

function renderLowBalance() {
    const tbody = document.getElementById('tbl-low-balance');
    tbody.innerHTML = '';
    Object.values(students).filter(s => parseInt(s.wallet_balance) <= 2).forEach(s => {
        const msg = encodeURIComponent(`مرحبًا، الطالب ${s.name} رصيده أوشك على الانتهاء (${s.wallet_balance} حصص فقط). يرجى التجديد.\nBrothers Studio`);
        tbody.innerHTML += `<tr><td>${s.uid}</td><td>${s.name}</td><td><span class="badge badge-red">${s.wallet_balance}</span></td><td><a class="wa-btn" href="https://wa.me/${s.parent_phone}?text=${msg}" target="_blank"><i class="fa-brands fa-whatsapp"></i> تواصل</a></td></tr>`;
    });
    if (!tbody.innerHTML) tbody.innerHTML = '<tr><td colspan="4" style="text-align:center;color:var(--muted)">لا يوجد طلاب برصيد منخفض</td></tr>';
}

// ===== ADD STUDENT =====
function addStudent(e) {
    e.preventDefault();
    const uid = String(Math.floor(1000000 + Math.random()*9000000));
    const payload = {
        uid, name: document.getElementById('st-name').value.trim(),
        student_phone: document.getElementById('st-phone').value.trim(),
        parent_phone: document.getElementById('st-parent').value.trim(),
        wallet_balance: parseInt(document.getElementById('st-wallet').value),
        center_reference: document.getElementById('st-center').value,
        group_reference: document.getElementById('st-group').value,
        registration_date: new Date().toLocaleDateString('ar-EG')
    };
    db.ref(`students_directory/${uid}`).set(payload).then(() => {
        log(`تسجيل طالب جديد: ${payload.name} (${uid})`);
        ['st-name','st-phone','st-parent'].forEach(id => document.getElementById(id).value='');
        document.getElementById('st-wallet').value='0';
        showIDCard(payload);
    });
}

function showIDCard(s) {
    db.ref('app_configurations/professor_identity').once('value', snap => {
        const prof = snap.val() || '—';
        document.getElementById('id-prof-name').innerText = prof;
        document.getElementById('id-st-name').innerText = s.name;
        document.getElementById('id-st-code').innerText = `كود الطالب: ${s.uid}`;
        const qz = document.getElementById('id-qr-zone');
        qz.innerHTML = '';
        new QRCode(qz, { text:s.uid, width:100, height:100, colorDark:'#0f172a', colorLight:'#fff' });
        document.getElementById('id-card-zone').style.display='block';
        document.getElementById('id-empty').style.display='none';
        document.getElementById('print-btn').style.display='inline-flex';
        showView('students');
    });
}

// ===== ATTENDANCE =====
function onScanKey(e) {
    if (e.key === 'Enter') { e.preventDefault(); manualScan(); }
}

function manualScan() {
    const code = document.getElementById('scan-input').value.trim();
    document.getElementById('scan-input').value = '';
    if (code) processAttendance(code);
}

function processAttendance(code) {
    const groupId = document.getElementById('att-group').value;
    const fb = document.getElementById('att-feedback');
    fb.style.display='block';
    fb.className='feedback-alert';

    if (!groupId) {
        fb.classList.add('error');
        fb.innerText = '⚠️ اختر المجموعة أولاً';
        return;
    }
    const st = students[code];
    if (!st) {
        fb.classList.add('error');
        fb.innerText = `❌ الكود (${code}) غير موجود في النظام`;
        return;
    }
    if (st.group_reference !== groupId) {
        fb.classList.add('warning');
        fb.innerText = `⚠️ تنبيه: ${st.name} ينتمي لمجموعة مختلفة!`;
        return;
    }
    if (parseInt(st.wallet_balance) <= 0) {
        fb.classList.add('error');
        fb.innerText = `❌ مرفوض: رصيد ${st.name} صفر — لا يمكن تسجيل الحضور`;
        return;
    }
    const today = new Date().toISOString().slice(0,10);
    db.ref(`attendance_ledger/${groupId}/${today}/${code}`).once('value', snap => {
        if (snap.exists()) {
            fb.classList.add('warning');
            fb.innerText = `⚠️ ${st.name} تم تسجيل حضوره مسبقاً اليوم`;
        } else {
            const newBal = parseInt(st.wallet_balance) - 1;
            db.ref(`students_directory/${code}/wallet_balance`).set(newBal).then(() => {
                db.ref(`attendance_ledger/${groupId}/${today}/${code}`).set({
                    timestamp: new Date().toLocaleTimeString('ar-EG'),
                    status:'حضور معتمد', assistant: session?.name||'—'
                }).then(() => {
                    fb.classList.add('success');
                    fb.innerText = `✅ تم تسجيل حضور ${st.name} — الرصيد المتبقي: ${newBal} حصة`;
                    log(`تسجيل حضور: ${st.name} (${code}) — رصيد جديد: ${newBal}`);
                    syncAttendance();
                });
            });
        }
    });
}

function syncAttendance() {
    const groupId = document.getElementById('att-group')?.value;
    const tbody = document.getElementById('report-tbody');
    if (!tbody) return;
    if (!groupId) { tbody.innerHTML=''; return; }

    const today = new Date().toISOString().slice(0,10);
    db.ref(`attendance_ledger/${groupId}/${today}`).on('value', snap => {
        const attended = snap.val() || {};
        tbody.innerHTML = '';
        let pres=0, abs=0;

        Object.values(students).filter(s=>s.group_reference===groupId).forEach(s => {
            if (attended[s.uid]) {
                pres++;
                tbody.innerHTML += `<tr>
                    <td>${s.uid}</td><td>${s.name}</td>
                    <td><span class="badge badge-green">✅ حاضر (${attended[s.uid].timestamp})</span></td>
                    <td>${s.wallet_balance} حصة</td><td>—</td>
                </tr>`;
            } else {
                abs++;
                const msg = encodeURIComponent(`مرحبًا ولي الأمر،\nالطالب ${s.name} لم يحضر الحصة اليوم.\nيرجى المتابعة.\nBrothers Studio`);
                tbody.innerHTML += `<tr>
                    <td>${s.uid}</td><td>${s.name}</td>
                    <td><span class="badge badge-red">❌ غائب</span></td>
                    <td>${s.wallet_balance} حصة</td>
                    <td><a class="wa-btn" href="https://wa.me/${s.parent_phone}?text=${msg}" target="_blank" onclick="logWA('${s.name}')"><i class="fa-brands fa-whatsapp"></i> متابعة</a></td>
                </tr>`;
            }
        });
        document.getElementById('s-present').innerText = pres;
        document.getElementById('s-absent').innerText = abs;
    });
}

function logWA(name) {
    log(`إرسال متابعة واتساب لولي أمر: ${name}`);
}

// ===== WALLET =====
function previewWallet() {
    const code = document.getElementById('w-code').value.trim();
    const st = students[code];
    const pv = document.getElementById('wallet-preview');
    if (st) {
        document.getElementById('w-st-name').innerText = st.name;
        document.getElementById('w-balance').innerText = st.wallet_balance;
        pv.style.display='block';
    } else {
        pv.style.display='none';
    }
}

function doWallet() {
    const code = document.getElementById('w-code').value.trim();
    const action = document.getElementById('w-action').value;
    const amount = parseInt(document.getElementById('w-amount').value);
    const fb = document.getElementById('wallet-feedback');
    const st = students[code];
    if (!st) { fb.style.color='var(--red)'; fb.innerText='كود غير موجود'; return; }
    if (isNaN(amount)||amount<0) { fb.style.color='var(--red)'; fb.innerText='قيمة غير صحيحة'; return; }

    let newBal = parseInt(st.wallet_balance);
    if (action==='add') newBal += amount;
    else if (action==='deduct') newBal = Math.max(0, newBal-amount);
    else newBal = amount;

    db.ref(`students_directory/${code}/wallet_balance`).set(newBal).then(() => {
        fb.style.color='var(--green)';
        fb.innerText = `✅ تم تعديل رصيد ${st.name} إلى ${newBal} حصة`;
        log(`تعديل رصيد: ${st.name} → ${newBal} حصة`);
        previewWallet();
    });
}

// ===== EXPORT =====
function exportReport(fmt) {
    const gid = document.getElementById('att-group').value;
    if (!fmt||!gid) { alert('اختر مجموعة أولاً'); return; }
    if (fmt==='pdf') { window.print(); return; }
    const today = new Date().toISOString().slice(0,10);
    db.ref(`attendance_ledger/${gid}/${today}`).once('value', snap => {
        const att = snap.val()||{};
        let csv = '\uFEFF كود الطالب,الاسم,الحالة,الرصيد\n';
        Object.values(students).filter(s=>s.group_reference===gid).forEach(s => {
            const status = att[s.uid] ? `حاضر (${att[s.uid].timestamp})` : 'غائب';
            csv += `"${s.uid}","${s.name}","${status}","${s.wallet_balance}"\n`;
        });
        const a = document.createElement('a');
        a.href = encodeURI('data:text/csv;charset=utf-8,'+csv);
        a.download = `تقرير_حضور_${today}.csv`;
        a.click();
    });
}

// ===== CAMERA BARCODE =====
function openCamera() {
    document.getElementById('camera-modal').classList.add('open');
    document.getElementById('cam-status').innerText = 'جاري تشغيل الكاميرا...';

    // Try ZXing if available, fallback to manual
    if (window.ZXing) {
        startZXing();
    } else {
        // Fallback: use getUserMedia + BarcodeDetector if available
        startBarcodeDetector();
    }
}

async function startBarcodeDetector() {
    try {
        const stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode:'environment' } });
        cameraStream = stream;
        const video = document.getElementById('camera-video');
        video.srcObject = stream;
        await video.play();

        if ('BarcodeDetector' in window) {
            document.getElementById('cam-status').innerText = 'وجّه الكاميرا نحو الباركود...';
            const detector = new BarcodeDetector({ formats: ['qr_code','ean_13','ean_8','code_128','code_39','upc_a','upc_e'] });
            const scanLoop = setInterval(async () => {
                if (!cameraStream) { clearInterval(scanLoop); return; }
                try {
                    const codes = await detector.detect(video);
                    if (codes.length > 0) {
                        const val = codes[0].rawValue;
                        clearInterval(scanLoop);
                        closeCamera();
                        document.getElementById('cam-status').innerText = `تم: ${val}`;
                        processAttendance(val);
                    }
                } catch(e) {}
            }, 300);
        } else {
            document.getElementById('cam-status').innerText = '⚠️ الكاميرا تعمل — يرجى مسح الباركود يدوياً أو استخدام قارئ خارجي';
        }
    } catch(err) {
        document.getElementById('cam-status').innerText = '❌ لا يمكن الوصول للكاميرا — تأكد من منح الإذن';
    }
}

function startZXing() {
    navigator.mediaDevices.getUserMedia({ video: { facingMode:'environment' } }).then(stream => {
        cameraStream = stream;
        const video = document.getElementById('camera-video');
        video.srcObject = stream;
        video.play();

        codeReader = new ZXing.BrowserMultiFormatReader();
        document.getElementById('cam-status').innerText = 'وجّه الكاميرا نحو الباركود...';

        codeReader.decodeFromStream(stream, video, (result, err) => {
            if (result) {
                closeCamera();
                processAttendance(result.getText());
            }
        });
    }).catch(() => {
        document.getElementById('cam-status').innerText = '❌ تعذر الوصول للكاميرا';
    });
}

function closeCamera() {
    if (cameraStream) {
        cameraStream.getTracks().forEach(t => t.stop());
        cameraStream = null;
    }
    if (codeReader) {
        try { codeReader.reset(); } catch(e) {}
        codeReader = null;
    }
    document.getElementById('camera-video').srcObject = null;
    document.getElementById('camera-modal').classList.remove('open');
}
</script>
</body>
</html>